<nav class="bg-white  w-full z-20 top-0 left-0 border-b border-gray-200 ">
  <div class="container flex flex-wrap items-center justify-between mx-auto p-4">
    <a href="https://flowbite.com/" class="flex items-center">
      <span class="self-center text-2xl font-semibold whitespace-nowrap ">MNF</span>
    </a>
    <div class="flex md:order-2">
      <% if (user.name == '' ) { %>
        <a href="/login" type="button"
          class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center mr-3 md:mr-0 ">Login</a>
        <% }else{ %>
          <div class="flex items-center gap-2">
            <%= user.name %>
              <a href="/logout" type="button"
                class="text-white bg-gray-700 hover:bg-gray-800 focus:ring-4 focus:outline-none focus:ring-blue-200 font-medium rounded text-sm px-2 py-1 text-center mr-3 md:mr-0 ">Logout</a>
          </div>
          <% } %>
    </div>
    <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1">
      <ul
        class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-gray-100 rounded-lg bg-gray-50 md:flex-row md:space-x-8 md:mt-0 md:border-0 md:bg-white ">
        <li>
          <a href="/"
            class="block py-2 pl-3 pr-4 text-gray-900  <%= active === 'home' ? 'text-blue-800'  : 'text-gray-900' %> rounded hover:text-blue-900">Home</a>
        </li>

      <% if (user.isAdmin) { %>
        <li>
          <a href="/users"
            class="block py-2 pl-3 pr-4  rounded <%= active === 'users' ? 'text-blue-800'  : 'text-gray-900' %> ">Users</a>
        </li>
        <li>
          <a href="/subjects"
            class="block py-2 pl-3 pr-4 text-gray-900 rounded  <%= active === 'subjects' ? 'text-blue-800'  : 'text-gray-900' %> hover:text-blue-900">Subjects</a>
        </li>
        <li>
          <a href="/departments"
           class="block py-2 pl-3 pr-4 text-gray-900 rounded  <%= active === 'departments' ? 'text-blue-800'  : 'text-gray-900' %> hover:text-blue-900">Departments</a>
        </li>
      <% } %>

      </ul>
    </div>
  </div>
</nav>